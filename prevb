getgenv().SecureMode = true
local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()
 
local MainWindow = Rayfield:CreateWindow({
    Name = "ALVINO",
    LoadingTitle = "Sabar...",
    LoadingSubtitle = "by KONTOL",
    ConfigurationSaving = {
       Enabled = false,
       FolderName = nil, -- Create a custom folder for your hub/game
       FileName = "McDonalds Hub"
    }
 })

local MainTab = MainWindow:CreateTab("PELER", 7053923059) -- Title, Image

local Button = Tab:CreateButton({
    Name = "============= AUTO ALL =============",
    Callback = function(v)
        loadstring(game:HttpGet("https://raw.githubusercontent.com/JumbleBumble/Scripts/main/FruitBattlegrounds.lua"))()
    end,
})

